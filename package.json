{
  "name": "tank-arena",
  "version": "0.1.0",
  "private": true,
  "description": "Top-view multiplayer 2D tank battle prototype",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/client.js && node --check scripts/smoke.js",
    "smoke": "node scripts/smoke.js"
  },
  "engines": {
    "node": ">=20"
  }
}
